<?php
/* /**
 * @file
 * Contains \Drupal\ai_custom\AipopupController.
 */
 
/* namespace Drupal\ai_popup_feedback\Controller;
 
use Drupal\Core\Controller\ControllerBase;
use Symfony\Component\HttpFoundation\RedirectResponse;
 
class AipopupController extends ControllerBase {
  public function webformToNodeRedirect() {
  // Set redirect to previous page.
    $nid = $_GET['cntid'];
    if (!empty($nid)) {
      $node_path_alias = \Drupal::service('path.alias_manager')->getAliasByPath('/node/'.$nid);
      return new RedirectResponse($node_path_alias);
    }
  }
}  */
?>